import { style } from '@vanilla-extract/css';

export const printDisplay = style({
    height: '40px',
    marginBottom: '50px',
    textAlign: 'right',
    fontSize: '40px',
    fontWeight: '500',
});
