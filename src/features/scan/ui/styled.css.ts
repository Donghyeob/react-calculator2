import { style } from '@vanilla-extract/css';

export const scanInput = style({
    width: '100%',
    marginBottom: '30px',
    border: 'none',
    outline: 'none',
    textAlign: 'right',
    fontSize: '18px',
    color: 'rgba(0, 0, 0, 0.7)',
});
